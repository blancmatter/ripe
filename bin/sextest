#!/usr/bin/perl

use lib '/home/disrail/ripe/lib';
use ripe;

 
($pol, $lower, $upper) = ripe::mcerror($ARGV[0], $ARGV[1], $ARGV[2], $ARGV[3]);


#printf "Polarisation = %4.3f minus %3.3f plus %3.3f\n", $pol, $lower, $upper;
